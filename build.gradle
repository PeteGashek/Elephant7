subprojects {
    apply plugin: "java"

    repositories {
        mavenCentral()
    }
}

ext.buildAndDeployTaskGroup = "build and deploy"

ext.gsonVersion = "2.6.2"
ext.mysqlConnectorVersion = "5.1.38"
ext.servletApiVersion = "3.1.0"
ext.springVersion = "4.2.5.RELEASE"
ext.springBootVersion = "1.3.3.RELEASE"
ext.commonsLoggingVersion = "1.2"